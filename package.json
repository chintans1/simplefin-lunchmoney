{
  "name": "fidelity-lunchmoney",
  "version": "0.0.1",
  "description": "Using SimpleFIN Bridge, this should help you sync your Fidelity assets with LunchMoney.",
  "type": "commonjs",
  "compilerOptions": {
    "module": "CommonJS"
  },
  "main": "app.ts",
  "scripts": {
    "build": "esbuild --bundle --platform=node --outfile=build/run.js ./app.ts",
    "start": "node build/run.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chintan Shah",
  "license": "ISC",
  "dependencies": {
    "@types/node-localstorage": "^1.3.3",
    "dotenv": "^16.3.1",
    "esbuild": "^0.19.9",
    "lunch-money": "^0.5.0",
    "node-localstorage": "^3.0.5"
  },
  "devDependencies": {
    "@types/node": "^20.10.5"
  }
}
